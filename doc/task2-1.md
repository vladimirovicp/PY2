# 🐍 Магические методы и атрибуты Python
> Справочник по специальным (`dunder`) методам и атрибутам, сгруппированным по функциональным категориям. Каждый элемент отформатирован для быстрого чтения и удобной навигации.

---

## 1. 🏗️ Инициализация и создание объектов
| Метод/Атрибут | Сигнатура | Описание |
|:---|:---|:---|
| `__init__` | `__init__(self, ...)` | Инициализатор объекта. Вызывается автоматически после создания экземпляра. Аналог конструктора. |
| `__new__` | `__new__(cls, ...)` | Создаёт новый экземпляр класса. Вызывается **до** `__init__`, отвечает за выделение памяти и возврат объекта. |
| `__delattr__` | `__delattr__(self, name)` | Вызывается при удалении атрибута: `del obj.name`. Аналог встроенной функции `delattr()`. |
| `__setattr__` | `__setattr__(self, name, value)` | Вызывается при присваивании: `obj.name = value`. Аналог `setattr()`. |
| `__getattribute__` | `__getattribute__(self, name)` | Вызывается при **любом** доступе к атрибуту. Низкоуровневый хук, переопределяющий поведение оператора `.` |
| `__init_subclass__` | `__init_subclass__(cls, ...)` | Вызывается при создании подкласса. Позволяет кастомизировать поведение наследников без явного вызова `super()`. |

---

## 2. 🖼️ Представление и преобразование объекта
| Метод/Атрибут | Сигнатура | Описание |
|:---|:---|:---|
| `__str__` | `__str__(self)` | Возвращает человеко-читаемое строковое представление. Используется в `str()`, `print()`. |
| `__repr__` | `__repr__(self)` | Возвращает однозначное представление для разработчика. Используется в `repr()`, отладчиках. При отсутствии `__str__` выступает фоллбэком. |
| `__format__` | `__format__(self, format_spec)` | Определяет поведение при форматировании через `f"{obj:format}"` или `format(obj, format_spec)`. |
| `__dir__` | `__dir__(self)` | Возвращает список доступных атрибутов. Используется встроенной функцией `dir()`. |
| `__sizeof__` | `__sizeof__(self)` | Возвращает размер объекта в байтах. Используется функцией `sys.getsizeof()`. |

---

## 3. ⚖️ Сравнение и хеширование объектов
| Метод/Атрибут | Сигнатура | Описание |
|:---|:---|:---|
| `__eq__` | `__eq__(self, other)` | Оператор равенства: `obj1 == obj2` |
| `__ne__` | `__ne__(self, other)` | Оператор неравенства: `obj1 != obj2` |
| `__lt__` | `__lt__(self, other)` | Строгое неравенство: `obj1 < obj2` |
| `__le__` | `__le__(self, other)` | Нестрогое неравенство: `obj1 <= obj2` |
| `__gt__` | `__gt__(self, other)` | Строгое неравенство: `obj1 > obj2` |
| `__ge__` | `__ge__(self, other)` | Нестрогое неравенство: `obj1 >= obj2` |
| `__hash__` | `__hash__(self)` | Возвращает целочисленный хеш. Необходим для использования объекта в качестве ключа в `dict` или элемента `set`. ⚠️ Должен быть согласован с `__eq__`. |

---

## 4. 📦 Классы и метаданные
| Метод/Атрибут | Тип | Описание |
|:---|:---|:---|
| `__class__` | Атрибут | Ссылка на класс объекта: `obj.__class__` возвращает тип экземпляра. |
| `__module__` | Атрибут | Имя модуля, в котором определён класс или функция. |
| `__dict__` | Атрибут | Словарь, хранящий атрибуты объекта. Для экземпляров — собственные атрибуты, для классов — атрибуты класса. |
| `__doc__` | Атрибут | Строка документации (docstring). Задаётся тройными кавычками сразу после объявления. |
| `__annotations__` | Атрибут | Словарь аннотаций типов: ключи → имена переменных/параметров, значения → указанные типы. Используется статическими анализаторами и фреймворками. |
| `__weakref__` | Атрибут | Хранит список слабых ссылок на объект. Не препятствует удалению объекта сборщиком мусора. |

---

## 5. 📦 Сериализация и восстановление объектов
| Метод/Атрибут | Сигнатура | Описание |
|:---|:---|:---|
| `__getstate__` | `__getstate__(self)` | Определяет, какие данные объекта должны быть сериализованы. Часто используется с `pickle`. |
| `__reduce__` | `__reduce__(self)` | Возвращает инструкцию по сериализации/десериализации. Используется модулями вроде `pickle`. |
| `__reduce_ex__` | `__reduce_ex__(self, protocol)` | Расширенная версия `__reduce__`, принимающая номер протокола сериализации. |

---

## 6. 🌿 Наследование и поддержка абстрактных классов
| Метод/Атрибут | Сигнатура | Описание |
|:---|:---|:---|
| `__subclasshook__` | `__subclasshook__(cls, subclass)` | Определяет, считается ли класс родительским для другого. Позволяет реализовать структурное (duck-typing) наследование в `abc.ABCMeta`. |

---

### 💡 Рекомендации по использованию
| Практика | Пояснение |
|:---|:---|
| `__repr__` > `__str__` | Всегда реализуйте `__repr__`. Если `__str__` отсутствует, Python автоматически использует `__repr__`. |
| `__hash__` ↔ `__eq__` | Если переопределён `__eq__`, необходимо также переопределить `__hash__` или установить `__hash__ = None`. Иначе объект станет нехешируемым. |
| `__getattribute__` vs `__getattr__` | `__getattribute__` вызывается **всегда**. `__getattr__` — только если атрибут не найден. Ошибка в `__getattribute__` легко вызывает рекурсию. |
| `__new__` vs `__init__` | `__new__` создаёт объект, `__init__` его настраивает. `__new__` нужен для наследования от неизменяемых типов (`int`, `str`, `tuple`) или реализации синглтонов. |

---



```python
from typing import Union


class Glass:
    def __init__(self, capacity_volume: Union[int, float], occupied_volume: Union[int, float]):
        # TODO Распечатайте словарь атрибутов экземпляра класса в начале метода
        print(self.__dict__)

        self.capacity_volume = capacity_volume  # объем стакана
        self.occupied_volume = occupied_volume  # объем жидкости в стакане
        # TODO Распечатайте словарь атрибутов экземпляра класса в конце метода
        print(self.__dict__)


if __name__ == "__main__":
    glass = Glass(200, 100)
```

```python
class Glass:
    ...

if __name__ == "__main__":
    glass = Glass()

    print(type(glass))
    print(glass.__class__)

    print(type(glass) == glass.__class__)

```

```python
from typing import Union


class Glass:
    def __init__(self, capacity_volume: Union[int, float], occupied_volume: Union[int, float]):
        self.capacity_volume = capacity_volume  # объем стакана
        self.occupied_volume = occupied_volume  # объем жидкости в стакане

    def __str__(self) -> str:
        # TODO метод должен возвращать строку, которая содержит человеко-читаемую информацию)
        return f'Стакан объёмом {self.capacity_volume}. Объём жидкости = {self.occupied_volume}'



if __name__ == "__main__":
    glass = Glass(200, 100)  # экземпляр класса

    print(glass)  # Стакан объёмом 200. Объём жидкости = 100
```

```python

from typing import Union


class Glass:
    def __init__(self, capacity_volume: Union[int, float], occupied_volume: Union[int, float]):
        self.capacity_volume = capacity_volume  # объем стакана
        self.occupied_volume = occupied_volume  # объем жидкости в стакане

    def __str__(self) -> str:
        return f"Стакан объёмом {self.capacity_volume}. Объём жидкости = {self.occupied_volume}"

    # TODO реализуйте метод __repr__
    def __repr__(self) ->str:
        return f"Glass({self.capacity_volume}, {self.occupied_volume})"


if __name__ == "__main__":
    glass = Glass(200, 100)  # экземпляр класса

    print(glass)  # Стакан объёмом 200. Объём жидкости = 100
    print([Glass(i, i) for i in range(50, 251, 50)])  # [Glass(50, 50), Glass(100, 100), Glass(150, 150), Glass(200, 200), Glass(250, 250)]
```

